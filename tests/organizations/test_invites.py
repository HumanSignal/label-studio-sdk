# This file was auto-generated by Fern from our API Definition.

from label_studio_sdk import LabelStudio
from label_studio_sdk import AsyncLabelStudio
import typing
from ..utilities import validate_response


async def test_get_invite_link(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {"invite_url": "invite_url", "token": "token"}
    expected_types: typing.Any = {"invite_url": None, "token": None}
    response = client.organizations.invites.get_invite_link()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.organizations.invites.get_invite_link()
    validate_response(async_response, expected_response, expected_types)


async def test_revoke_invite(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert (
        client.organizations.invites.revoke_invite(email="email")  # type: ignore[func-returns-value]
        is None
    )

    assert (
        await async_client.organizations.invites.revoke_invite(email="email")  # type: ignore[func-returns-value]
        is None
    )


async def test_send_email(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert (
        client.organizations.invites.send_email(emails=["emails"], role="OW")  # type: ignore[func-returns-value]
        is None
    )

    assert (
        await async_client.organizations.invites.send_email(emails=["emails"], role="OW")  # type: ignore[func-returns-value]
        is None
    )
