# This file was auto-generated by Fern from our API Definition.

import typing

from label_studio_sdk.client import AsyncLabelStudio, LabelStudio

from .utilities import validate_response


async def test_list_(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = [
        {
            "id": 1,
            "state": "CO",
            "readable_state": "readable_state",
            "is_interactive": True,
            "url": "url",
            "error_message": "error_message",
            "title": "title",
            "auth_method": "NONE",
            "basic_auth_user": "basic_auth_user",
            "basic_auth_pass": "basic_auth_pass",
            "basic_auth_pass_is_set": "basic_auth_pass_is_set",
            "description": "description",
            "extra_params": {"key": "value"},
            "model_version": "model_version",
            "timeout": 1.1,
            "created_at": "2024-01-15T09:30:00Z",
            "updated_at": "2024-01-15T09:30:00Z",
            "auto_update": True,
            "project": 1,
        }
    ]
    expected_types: typing.Any = (
        "list",
        {
            0: {
                "id": "integer",
                "state": None,
                "readable_state": None,
                "is_interactive": None,
                "url": None,
                "error_message": None,
                "title": None,
                "auth_method": None,
                "basic_auth_user": None,
                "basic_auth_pass": None,
                "basic_auth_pass_is_set": None,
                "description": None,
                "extra_params": ("dict", {0: (None, None)}),
                "model_version": None,
                "timeout": None,
                "created_at": "datetime",
                "updated_at": "datetime",
                "auto_update": None,
                "project": "integer",
            }
        },
    )
    response = client.ml.list()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.ml.list()
    validate_response(async_response, expected_response, expected_types)


async def test_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "url": "url",
        "project": 1,
        "is_interactive": True,
        "title": "title",
        "description": "description",
        "auth_method": "NONE",
        "basic_auth_user": "basic_auth_user",
        "basic_auth_pass": "basic_auth_pass",
        "extra_params": {"key": "value"},
        "timeout": 1,
    }
    expected_types: typing.Any = {
        "url": None,
        "project": "integer",
        "is_interactive": None,
        "title": None,
        "description": None,
        "auth_method": None,
        "basic_auth_user": None,
        "basic_auth_pass": None,
        "extra_params": ("dict", {0: (None, None)}),
        "timeout": "integer",
    }
    response = client.ml.create()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.ml.create()
    validate_response(async_response, expected_response, expected_types)


async def test_get(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "id": 1,
        "state": "CO",
        "readable_state": "readable_state",
        "is_interactive": True,
        "url": "url",
        "error_message": "error_message",
        "title": "title",
        "auth_method": "NONE",
        "basic_auth_user": "basic_auth_user",
        "basic_auth_pass": "basic_auth_pass",
        "basic_auth_pass_is_set": "basic_auth_pass_is_set",
        "description": "description",
        "extra_params": {"key": "value"},
        "model_version": "model_version",
        "timeout": 1.1,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "auto_update": True,
        "project": 1,
    }
    expected_types: typing.Any = {
        "id": "integer",
        "state": None,
        "readable_state": None,
        "is_interactive": None,
        "url": None,
        "error_message": None,
        "title": None,
        "auth_method": None,
        "basic_auth_user": None,
        "basic_auth_pass": None,
        "basic_auth_pass_is_set": None,
        "description": None,
        "extra_params": ("dict", {0: (None, None)}),
        "model_version": None,
        "timeout": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "auto_update": None,
        "project": "integer",
    }
    response = client.ml.get(id=1)
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.ml.get(id=1)
    validate_response(async_response, expected_response, expected_types)


async def test_delete(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.ml.delete(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.ml.delete(id=1) is None  # type: ignore[func-returns-value]


async def test_update(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "url": "url",
        "project": 1,
        "is_interactive": True,
        "title": "title",
        "description": "description",
        "auth_method": "NONE",
        "basic_auth_user": "basic_auth_user",
        "basic_auth_pass": "basic_auth_pass",
        "extra_params": {"key": "value"},
        "timeout": 1,
    }
    expected_types: typing.Any = {
        "url": None,
        "project": "integer",
        "is_interactive": None,
        "title": None,
        "description": None,
        "auth_method": None,
        "basic_auth_user": None,
        "basic_auth_pass": None,
        "extra_params": ("dict", {0: (None, None)}),
        "timeout": "integer",
    }
    response = client.ml.update(id=1)
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.ml.update(id=1)
    validate_response(async_response, expected_response, expected_types)


async def test_predict_interactive(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.ml.predict_interactive(id=1, task=1) is None  # type: ignore[func-returns-value]

    assert await async_client.ml.predict_interactive(id=1, task=1) is None  # type: ignore[func-returns-value]


async def test_train(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.ml.train(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.ml.train(id=1) is None  # type: ignore[func-returns-value]


async def test_list_model_versions(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.ml.list_model_versions(id="id") is None  # type: ignore[func-returns-value]

    assert await async_client.ml.list_model_versions(id="id") is None  # type: ignore[func-returns-value]
