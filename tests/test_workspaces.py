# This file was auto-generated by Fern from our API Definition.

import typing

from label_studio_sdk.client import AsyncLabelStudio, LabelStudio

from .utilities import validate_response


async def test_list_(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = [
        {
            "id": 1,
            "title": "title",
            "description": "description",
            "is_public": True,
            "is_personal": True,
            "is_archived": True,
            "created_at": "2024-01-15T09:30:00Z",
            "updated_at": "2024-01-15T09:30:00Z",
            "created_by": 1,
            "color": "color",
        }
    ]
    expected_types: typing.Any = (
        "list",
        {
            0: {
                "id": "integer",
                "title": None,
                "description": None,
                "is_public": None,
                "is_personal": None,
                "is_archived": None,
                "created_at": "datetime",
                "updated_at": "datetime",
                "created_by": "integer",
                "color": None,
            }
        },
    )
    response = client.workspaces.list()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.workspaces.list()
    validate_response(async_response, expected_response, expected_types)


async def test_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "id": 1,
        "title": "title",
        "description": "description",
        "is_public": True,
        "is_personal": True,
        "is_archived": True,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "created_by": 1,
        "color": "color",
    }
    expected_types: typing.Any = {
        "id": "integer",
        "title": None,
        "description": None,
        "is_public": None,
        "is_personal": None,
        "is_archived": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "created_by": "integer",
        "color": None,
    }
    response = client.workspaces.create()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.workspaces.create()
    validate_response(async_response, expected_response, expected_types)


async def test_get(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "id": 1,
        "title": "title",
        "description": "description",
        "is_public": True,
        "is_personal": True,
        "is_archived": True,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "created_by": 1,
        "color": "color",
    }
    expected_types: typing.Any = {
        "id": "integer",
        "title": None,
        "description": None,
        "is_public": None,
        "is_personal": None,
        "is_archived": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "created_by": "integer",
        "color": None,
    }
    response = client.workspaces.get(id=1)
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.workspaces.get(id=1)
    validate_response(async_response, expected_response, expected_types)


async def test_delete(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.workspaces.delete(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.workspaces.delete(id=1) is None  # type: ignore[func-returns-value]


async def test_update(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response: typing.Any = {
        "id": 1,
        "title": "title",
        "description": "description",
        "is_public": True,
        "is_personal": True,
        "is_archived": True,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "created_by": 1,
        "color": "color",
    }
    expected_types: typing.Any = {
        "id": "integer",
        "title": None,
        "description": None,
        "is_public": None,
        "is_personal": None,
        "is_archived": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "created_by": "integer",
        "color": None,
    }
    response = client.workspaces.update(id=1)
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.workspaces.update(id=1)
    validate_response(async_response, expected_response, expected_types)
