# This file was auto-generated by Fern from our API Definition.

from label_studio_sdk import MlBackend
from label_studio_sdk.client import AsyncLabelStudio, LabelStudio

from .utilities import validate_response


async def test_api_ml_list(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response = [
        {
            "id": 1,
            "state": "CO",
            "readable_state": "readable_state",
            "is_interactive": True,
            "url": "url",
            "error_message": "error_message",
            "title": "title",
            "auth_method": "NONE",
            "basic_auth_user": "basic_auth_user",
            "basic_auth_pass": "basic_auth_pass",
            "basic_auth_pass_is_set": "basic_auth_pass_is_set",
            "description": "description",
            "extra_params": {"extra_params": {"key": "value"}},
            "model_version": "model_version",
            "timeout": 1.1,
            "created_at": "2024-01-15T09:30:00Z",
            "updated_at": "2024-01-15T09:30:00Z",
            "auto_update": True,
            "project": 1,
        }
    ]
    expected_types = (
        "list",
        {
            0: {
                "id": "integer",
                "state": None,
                "readable_state": None,
                "is_interactive": None,
                "url": None,
                "error_message": None,
                "title": None,
                "auth_method": None,
                "basic_auth_user": None,
                "basic_auth_pass": None,
                "basic_auth_pass_is_set": None,
                "description": None,
                "extra_params": ("dict", {0: (None, None)}),
                "model_version": None,
                "timeout": None,
                "created_at": "datetime",
                "updated_at": "datetime",
                "auto_update": None,
                "project": "integer",
            }
        },
    )
    response = client.machine_learning.api_ml_list()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.machine_learning.api_ml_list()
    validate_response(async_response, expected_response, expected_types)


async def test_api_ml_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response = {"project": 1, "url": "url"}
    expected_types = {"project": "integer", "url": None}
    response = client.machine_learning.api_ml_create()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.machine_learning.api_ml_create()
    validate_response(async_response, expected_response, expected_types)


async def test_api_ml_read(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response = {
        "id": 1,
        "state": "CO",
        "readable_state": "readable_state",
        "is_interactive": True,
        "url": "url",
        "error_message": "error_message",
        "title": "title",
        "auth_method": "NONE",
        "basic_auth_user": "basic_auth_user",
        "basic_auth_pass": "basic_auth_pass",
        "basic_auth_pass_is_set": "basic_auth_pass_is_set",
        "description": "description",
        "extra_params": {"extra_params": {"key": "value"}},
        "model_version": "model_version",
        "timeout": 1.1,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "auto_update": True,
        "project": 1,
    }
    expected_types = {
        "id": "integer",
        "state": None,
        "readable_state": None,
        "is_interactive": None,
        "url": None,
        "error_message": None,
        "title": None,
        "auth_method": None,
        "basic_auth_user": None,
        "basic_auth_pass": None,
        "basic_auth_pass_is_set": None,
        "description": None,
        "extra_params": ("dict", {0: (None, None)}),
        "model_version": None,
        "timeout": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "auto_update": None,
        "project": "integer",
    }
    response = client.machine_learning.api_ml_read(id=1)
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.machine_learning.api_ml_read(id=1)
    validate_response(async_response, expected_response, expected_types)


async def test_api_ml_delete(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.machine_learning.api_ml_delete(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.machine_learning.api_ml_delete(id=1) is None  # type: ignore[func-returns-value]


async def test_api_ml_partial_update(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    expected_response = {
        "id": 1,
        "state": "CO",
        "readable_state": "readable_state",
        "is_interactive": True,
        "url": "url",
        "error_message": "error_message",
        "title": "title",
        "auth_method": "NONE",
        "basic_auth_user": "basic_auth_user",
        "basic_auth_pass": "basic_auth_pass",
        "basic_auth_pass_is_set": "basic_auth_pass_is_set",
        "description": "description",
        "extra_params": {"extra_params": {"key": "value"}},
        "model_version": "model_version",
        "timeout": 1.1,
        "created_at": "2024-01-15T09:30:00Z",
        "updated_at": "2024-01-15T09:30:00Z",
        "auto_update": True,
        "project": 1,
    }
    expected_types = {
        "id": "integer",
        "state": None,
        "readable_state": None,
        "is_interactive": None,
        "url": None,
        "error_message": None,
        "title": None,
        "auth_method": None,
        "basic_auth_user": None,
        "basic_auth_pass": None,
        "basic_auth_pass_is_set": None,
        "description": None,
        "extra_params": ("dict", {0: (None, None)}),
        "model_version": None,
        "timeout": None,
        "created_at": "datetime",
        "updated_at": "datetime",
        "auto_update": None,
        "project": "integer",
    }
    response = client.machine_learning.api_ml_partial_update(id=1, request=MlBackend(url="url", project=1))
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.machine_learning.api_ml_partial_update(
        id=1, request=MlBackend(url="url", project=1)
    )
    validate_response(async_response, expected_response, expected_types)


async def test_api_ml_interactive_annotating_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.machine_learning.api_ml_interactive_annotating_create(id=1, task=1) is None  # type: ignore[func-returns-value]

    assert await async_client.machine_learning.api_ml_interactive_annotating_create(id=1, task=1) is None  # type: ignore[func-returns-value]


async def test_api_ml_predict_test_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.machine_learning.api_ml_predict_test_create(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.machine_learning.api_ml_predict_test_create(id=1) is None  # type: ignore[func-returns-value]


async def test_api_ml_train_create(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.machine_learning.api_ml_train_create(id=1) is None  # type: ignore[func-returns-value]

    assert await async_client.machine_learning.api_ml_train_create(id=1) is None  # type: ignore[func-returns-value]


async def test_api_ml_versions_read(client: LabelStudio, async_client: AsyncLabelStudio) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.machine_learning.api_ml_versions_read(id="id") is None  # type: ignore[func-returns-value]

    assert await async_client.machine_learning.api_ml_versions_read(id="id") is None  # type: ignore[func-returns-value]
