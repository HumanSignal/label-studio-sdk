imports:
  root: ../../__package__.yml
types:
  BulkPostResponse:
    properties:
      assignments: optional<integer>
    source:
      openapi: openapi/openapi.yaml
  BulkDeleteResponse:
    properties:
      unassignments: optional<integer>
    source:
      openapi: openapi/openapi.yaml
service:
  auth: false
  base-path: ''
  endpoints:
    post:
      path: /api/projects/{id}/members/bulk/
      method: POST
      auth:
        - Token: []
      docs: |-
        <Card href="https://humansignal.com/goenterprise">
                <img style="pointer-events: none; margin-left: 0px; margin-right: 0px;" src="https://docs.humansignal.com/images/badge.svg" alt="Label Studio Enterprise badge"/>
                <p style="margin-top: 10px; font-size: 14px;">
                    This endpoint is not available in Label Studio Community Edition. [Learn more about Label Studio Enterprise](https://humansignal.com/goenterprise)
                </p>
            </Card>
        Assign project members in bulk.
      source:
        openapi: openapi/openapi.yaml
      path-parameters:
        id: integer
      display-name: ✨ Bulk assign project members
      request:
        name: ProjectMemberBulkAssignRequest
        query-parameters:
          last_activity__gte:
            type: optional<string>
            docs: >-
              Filter by last activity (ISO 8601 formatted date). Only when
              all=True.
          role:
            type: optional<string>
            docs: >-
              Filter by role, project roles take precedence over organization
              roles. Only when all=True. (comma-separated values)
          search:
            type: optional<string>
            docs: >-
              Search term for filtering members by name, email, or username.
              Only when all=True.
        body:
          properties:
            all: boolean
            excluded: optional<list<integer>>
            included: optional<list<integer>>
            roles: optional<list<root.ProjectMemberBulkAssignRolesRequest>>
        content-type: application/json
      response:
        docs: ''
        type: BulkPostResponse
        status-code: 200
      examples:
        - path-parameters:
            id: 1
          query-parameters:
            last_activity__gte: last_activity__gte
            role: role
            search: search
          request:
            all: true
          response:
            body:
              assignments: 1
      audiences:
        - public
    delete:
      path: /api/projects/{id}/members/bulk/
      method: DELETE
      auth:
        - Token: []
      docs: >-
        <Card href="https://humansignal.com/goenterprise">
                <img style="pointer-events: none; margin-left: 0px; margin-right: 0px;" src="https://docs.humansignal.com/images/badge.svg" alt="Label Studio Enterprise badge"/>
                <p style="margin-top: 10px; font-size: 14px;">
                    This endpoint is not available in Label Studio Community Edition. [Learn more about Label Studio Enterprise](https://humansignal.com/goenterprise)
                </p>
            </Card>
        Unassign project members in bulk. Allows the same request body as bulk
        assign.
      source:
        openapi: openapi/openapi.yaml
      path-parameters:
        id: integer
      display-name: ✨ Bulk unassign project members
      request:
        name: BulkDeleteRequest
        query-parameters:
          last_activity__gte:
            type: optional<string>
            docs: >-
              Filter by last activity (ISO 8601 formatted date). Only when
              all=True.
          role:
            type: optional<string>
            docs: >-
              Filter by role, project roles take precedence over organization
              roles. Only when all=True. (comma-separated values)
          search:
            type: optional<string>
            docs: >-
              Search term for filtering members by name, email, or username.
              Only when all=True.
      response:
        docs: ''
        type: BulkDeleteResponse
        status-code: 200
      examples:
        - path-parameters:
            id: 1
          query-parameters:
            last_activity__gte: last_activity__gte
            role: role
            search: search
          response:
            body:
              unassignments: 1
      audiences:
        - public
  source:
    openapi: openapi/openapi.yaml
