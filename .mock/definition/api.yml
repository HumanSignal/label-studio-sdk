name: api
error-discrimination:
  strategy: status-code
display-name: Label Studio API
auth-schemes:
  Token:
    header: Authorization
    name: api_key
    type: string
    env: LABEL_STUDIO_API_KEY
    prefix: 'Token '
auth: Token
