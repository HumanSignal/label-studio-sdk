# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.unchecked_base_model import UncheckedBaseModel


class OrganizationMemberSkillsOption(UncheckedBaseModel):
    """
    An enumerable Contributor Setup answer available to the Skills filter.

    ``section_key`` / ``field_key`` / ``code`` are the filter identity. Titles are live
    Contributor Setup names. ``parent_code`` is set only when that ancestor is also an
    option on the same question, matching ``OrganizationTaxonomyNodeSerializer``.
    """

    code: str
    field_key: str
    field_label: str
    label: str
    parent_code: typing.Optional[str] = None
    section_key: str
    section_title: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
